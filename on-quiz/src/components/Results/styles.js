const image = {
  width: '100%',
}

const section = {
  backgroundColor: 'var(--light-grey)',
  padding: '3rem',
  marginBottom: '2rem',
}

const link = {
  textAlign: 'center',
  width: '60%',
  margin: '0 auto',
  backgroundColor: 'var(--dark-blue-green)',
}

const restartContainer = {
  margin: '2rem 0 4rem 0',
  textAlign: 'center',
}

const restart = {
  color: 'var(--dark-grey)',
  border: 'none',
  background: 'none',
}

const styles = { image, section, link, restartContainer, restart }

export default styles