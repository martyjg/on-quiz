const container = {
  backgroundColor: 'var(--dark-grey)',
  color: 'var(--white)',
  textAlign: 'center',
}

const introText = {
  fontFamily: 'arial',
  textTransform: 'uppercase',
  letterSpacing: '0.2rem',
  lineHeight: 1.75,
  fontSize: '0.8rem',
  marginTop: '1.5rem',
}

const loadingText = {
  fontSize: '2rem',
}

const styles = { container, introText, loadingText }

export default styles