const container = {
  backgroundColor: 'var(--dark-grey)',
  color: 'var(--white)',
  textAlign: 'center',
}

const introText = {
  fontFamily: 'arial',
  textTransform: 'uppercase',
  letterSpacing: '0.2rem',
  lineHeight: 1.75,
  fontSize: '0.8rem',
  marginTop: '1.5rem',
}

const question = {
  fontSize: '2rem',
  margin: '10rem 0',
  padding: '0 1rem',
}

const buttonContainer = {
  display: 'flex',
  margin: '0 -1rem',
}

const button = {
  margin: '0 1rem',
  width: '50%',
  border: '1px solid var(--white)',
  fontFamily: 'arial',
  fontSize: '1.5rem',
}

const loadingText = {
  fontSize: '2rem',
}

const styles = { container, introText, question, buttonContainer, button, loadingText }

export default styles