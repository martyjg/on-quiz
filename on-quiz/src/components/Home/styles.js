const heading = {
  fontSize: '3.5rem',
  marginTop: '6rem',
}

const button = {
  fontSize: '2rem',
  backgroundColor: 'var(--black)',
  textDecoration: 'none',
  display: 'inline-flex',
}

const container = {
  backgroundImage: 'linear-gradient(0deg, rgba(238,238,238,1) 0%, rgba(255,255,255,0.24555759803921573) 20%, rgba(246,253,255,0) 100%), url("images/Background Image Start Screen.png")',
  backgroundColor: 'var(--light-grey)',
  backgroundRepeat: 'no-repeat',
  backgroundSize: '140%',
  backgroundPosition: '0 100%',
}

const smallText = {
  fontSize: '1.2rem',
  fontFamily: 'helvetica',
  color: 'var(--dark-grey)',
}

const styles = { heading, button, container, smallText }

export default styles